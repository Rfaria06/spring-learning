spring.config.import=file:security/section4/springsecsection4/src/main/resources/.env[.properties]

spring.application.name=${SPRING_APP_NAME:eazybank}

logging.pattern.console=${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}

spring.datasource.url=jdbc:mariadb://${DATABASE_HOST:nas.axanet.local}:${DATABASE_PORT:3306}/${DATABASE_NAME:eazybank}
spring.datasource.username=${DATABASE_USERNAME:raul}
spring.datasource.password=${DATABASE_PASSWORD}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}
